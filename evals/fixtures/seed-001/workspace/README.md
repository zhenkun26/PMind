# User Directory Fixture

This synthetic Ruby application models an internal user directory. It has no
database, network access, framework dependency, or production data.

The requested feature is intentionally absent. Read `CONTRACT.md`, inspect the
existing domain classes, and implement only the export behavior authorized by
the task you receive.

Run the existing base checks with:

```sh
ruby test/user_directory_test.rb
```
