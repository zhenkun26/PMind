# Local Work Tracker

Create one directory per approved feature:

```text
.scratch/<feature-slug>/spec.md
.scratch/<feature-slug>/issues/01-<ticket>.md
```

See `docs/agents/issue-tracker.md`. This directory is intentionally tracked so
local decisions and task history survive model compaction and handoff.
