---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADR's and glossary) as we go.
---

Invoke `$grilling` and `$domain-modeling` for the same request. Use their
outputs together: `grilling` drives the interview while `domain-modeling`
updates the glossary and any justified ADRs.
